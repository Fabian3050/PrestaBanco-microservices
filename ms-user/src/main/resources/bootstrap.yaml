eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-service:8761/eureka/

spring:
  application:
    name: ms-user
  cloud:
    config:
      enabled: true
      uri: http://config-server:8081