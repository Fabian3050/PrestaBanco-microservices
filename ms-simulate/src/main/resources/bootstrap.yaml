spring:
  application:
    name: ms-simulate
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://ms-simulate:5432/simulatedb
    username: postgres
    password: 0987
  jpa:
    hibernate:
      ddl-auto: update